import { PieceDirective } from './piece.directive';

describe('PieceDirective', () => {
  it('should create an instance', () => {
    const directive = new PieceDirective();
    expect(directive).toBeTruthy();
  });
});
